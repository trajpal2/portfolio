node_modules
.git
out
dist
.env
.env.local
.DS_Store

# Explicit: never commit dev server / Turbopack cache (single .sst files can exceed GitHub's 100 MB limit).
.next/dev/
.next/cache/

# Runtime site counters (updated by API routes — no Redis)
data/site-stats.json
# Legacy resume-only counter; migrated into site-stats.json on first read if present
data/resume-downloads.json